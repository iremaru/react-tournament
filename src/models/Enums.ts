export enum FighterPosition {
	RIGHT = 'right',
	LEFT = 'left',
}

export enum FighterActionState {
	IDLE = 'idle',
	ATTACKING = 'attacking',
	DEFENDING = 'defending',
	GETTING_DAMAGE = 'damage',
	DEAD = 'dead',
}